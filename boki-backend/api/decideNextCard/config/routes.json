{
  "routes": [
    {
      "method": "GET",
      "path": "/decideNextCard",
      "handler": "decideNextCard.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/decideNextCard/:deckID",
      "handler": "decideNextCard.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}
